import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.videoeditor.instagram',
  appName: 'Instagram Video Editor',
  webDir: 'dist'
};

export default config;
